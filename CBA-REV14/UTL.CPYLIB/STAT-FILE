* DESC - Defines file stats demographics and parameters
*        this file is a Q-FILE to last fils save file stats preformed
*        from port (00).
*******************************
*  STAT-FILE FILE DEFINITION  *
*      KEY = SEQUENCE#        *
*******************************
 STAT.REC.SIZE = 40
 DIM STAT.REC(40)
 EQU STA.A.F.F.NAME   TO STAT.REC(1) ;* 1) Account name:"*":File name
                                     ;* 2) Account name:"*":File name:"*DL/ID"
 EQU STA.LEVEL        TO STAT.REC(2) ;* System level of specified file
                                     ;*  0 = system dictionary
                                     ;*  1 = Master dictionary
                                     ;*  2 = Data dictionary
                                     ;*  3 = Data file
 EQU STA.BASE         TO STAT.REC(3) ;* Beginning frame number of the file.
 EQU STA.MOD          TO STAT.REC(4) ;* Modulo of the file.
 EQU STA.SEP          TO STAT.REC(5) ;* Separation (number of frames per group) of the file.
 EQU STA.ITEMS        TO STAT.REC(6) ;* Total number of items in the file.
 EQU STA.SIZE         TO STAT.REC(7) ;* Size of the file in bytes.
 EQU STA.MAXI         TO STAT.REC(8) ;* Size (in bytes) of the largest item in the file.
 EQU STA.MINI         TO STAT.REC(9) ;* Size (in bytes) of the smallest item in the file.
 EQU STA.MAXGP        TO STAT.REC(10);* Maximum number of items found in a group in the file.
 EQU STA.MINGP        TO STAT.REC(11);* Minimum number of items found in a group in the file.
 EQU STA.FRAMES       TO STAT.REC(12);* Total frames, including linked, used by the file.
 EQU STA.25.PER       TO STAT.REC(13);* Number of groups that are 25% full or less.
 EQU STA.50.PER       TO STAT.REC(14);* Number of groups that are 25% to 50% full.
 EQU STA.75.PER       TO STAT.REC(15);* Number of groups that are 50% to 75% full.
 EQU STA.100.PER      TO STAT.REC(16);* Number of groups that are 75% to 100% full.
 EQU STA.125.PER      TO STAT.REC(17);* Primary space of groups in this category is full and 25% additional linked space is required.
 EQU STA.150.PER      TO STAT.REC(18);* Primary space of groups in this category is full and 50% additional linked space is required.
 EQU STA.200.PER      TO STAT.REC(19);* Primary space of groups in tis category is full and additional linked space equals the primary space.
 EQU STA.GER.200.PER  TO STAT.REC(20) ;* Primary space of groups in this category is full and additional linked space greater than the primary space.
 EQU STA.GFE          TO STAT.REC(21) ;* Group format errors, if any, in the file.
