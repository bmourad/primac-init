SUBROUTINE WIN.CURSOR(TYPE, TURN.ON)
* Set the cursor shape and if its on or off
* Compile for: GENERIC AP MD ME PI.SUB PR.SUB SQ UD UL UP UV UC IN GA
* (C) Copyright IBM Corporation 2003. All rights reserved
* Copyright (c) 1991-93. Impact Business Systems
*
TURN.ON = OCONV(TURN.ON,"MCU")                            
*
IF TYPE # "" THEN CALL WIN.COMSUB("Set Cursor='":TYPE:"'")
*
IF TURN.ON = "ON" OR TURN.ON = "1" THEN
   CALL WIN.COMSUB("Display CursorOn")
END ELSE                                  
   IF TURN.ON # '' THEN CALL WIN.COMSUB("Display CursorOff")
END
*
RETURN
*
END
