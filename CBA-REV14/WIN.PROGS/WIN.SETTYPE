SUBROUTINE WIN.SETTYPE(FNAME, TYPE, CREATOR)
* Set the type and creator of a Macintosh file
* Compile for: GENERIC AP MD ME PI.SUB PR.SUB SQ UD UL UP UV UC IN GA
* (C) Copyright IBM Corporation 2003. All rights reserved
*
SCRIPT = 'If DesktopType = "MAC" Then'
IF TYPE # "" THEN SCRIPT<-1> = 'File SetType "':FNAME:'","':TYPE:'"'
IF CREATOR # "" THEN SCRIPT<-1> = 'File SetCreator "':FNAME:'","':CREATOR:'"'
SCRIPT<-1> = 'Endif'
*
CALL WIN.HSCRIPT(SCRIPT)
*
RETURN
*
END
