SUBROUTINE WIN.SPUSH
* Subroutine to store the current screen in memory
* Compile for: GENERIC AP MD ME PI.SUB PR.SUB SQ UD UL UP UV UC IN GA
* (C) Copyright IBM Corporation 2003. All rights reserved
* Copyright (c) 1991-93. Impact Business Systems
*
CALL WIN.GETVAR("scrn_stack",STACK)
IF STACK = '' THEN STACK = 0
STACK = STACK + 1
CALL WIN.COMSUB("Screen Store scrn":STACK)
CALL WIN.SETVAR("scrn_stack",STACK)
*
RETURN
*
END
