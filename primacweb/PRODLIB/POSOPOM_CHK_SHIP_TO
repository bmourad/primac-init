SUBROUTINE POSOPOM_CHK_SHIP_TO
********************************************************************************
*   Program name :- POSOPOM_CHK_SHIP_TO
*   Created:- 11/17/2005
*   Programmer :- Suhail Hussain
*   Doc:- This method is to validate if a ShipTo ID specified by user exists
*------------------------------------------------------------------------------*
*

* In Properties:
* --------------
*  
*
* Out Properties:
* ---------------
*  
********************************************************************************
$INCLUDE WWINSERT RBO.H
$INCLUDE ICS.CPYLIB WAREHOUSE
* Insert method code here

OPEN "","WAREHOUSE" TO WAREHOUSE ELSE ERRMSG = "CANNOT OPEN WAREHOUSE FILE";GOTO 91000

   STATUS = RBO.getProperty("","PMCProperty",PMCProperty)
   STATUS = RBO.getProperty("","POS_SHIP_TO",SHIPTO_ID)
  
   CONO = PMCProperty<1,4>

   MATREAD WHSE.REC FROM WAREHOUSE,CONO:SHIPTO_ID ELSE
      ERRMSG = "Can't locate Warehouse"
      GOTO 91000
   END
RETURN

* End of method code
91000*
   STATUS = RBO.setProperty("","ServerStatus",1)
   STATUS = RBO.setProperty("","ServerMessage",ERRMSG)
RETURN

